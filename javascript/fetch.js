const myImage = document.querySelector('img');

fetch("../images/nahida_draw.png")
.then(function(response) {
  return response.blob();
})
.then(function(myBlob) {
  const objectURL = URL.createObjectURL(myBlob);
  myImage.src = objectURL;
});